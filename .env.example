# 複製此檔為 .env 並填入真實值;.env 不會進入 git
# cp .env.example .env

# 資料庫(SQLite 檔案路徑)
DATABASE_URL=sqlite:///./data/iqc.db

# 上傳檔案(照片、PDF 報告)存放目錄
UPLOAD_DIR=./data/uploads

# 簽發登入 session 用的密鑰:請改成隨機長字串(例:openssl rand -hex 32)
SECRET_KEY=change-me-to-a-random-hex-string

# 後端服務埠
PORT=8000
